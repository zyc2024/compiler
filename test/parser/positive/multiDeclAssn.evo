

type ColorPoint = {
    int v;
}

type Color = {
    ColorPoint[] color;
}


int, bool, Color configData() {
    return 
        55,
        false,
        Color(
            color=[ColorPoint(v=255), ColorPoint(v=255), ColorPoint(v=255)]
        );
    
}

int, int, int getTriplet() {
    return 1,2,88;
}

int main() {
    int x, y, z;
    x, y, z = getTriplet();
    bool n2, n6, n, nn;
    Color c;
    x, n, c = configData();
}